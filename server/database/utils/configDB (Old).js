// Declare the variables for database name, username, and password.
const dbName = 'photography';
const dbUser = 'postgres';
const dbPwd = 'admin';

// Export the variables 
module.exports = {
    dbName,
    dbUser,
    dbPwd
};
